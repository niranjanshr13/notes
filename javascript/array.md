
```
array's 'indexing'
-----------------
var x = [];   <--- check the below; without a 'var'
x[0] = 12;
x[1] = apple;
console.log(x[0])
---
var x = ['a',4,'ad'];
console.log(x[2])
---
length of array
---------------
x.length
  - console.log(x.length);
  - .reverse())
  - .join()
  - .sort()
  - Math.round(x)
  - Math.max(1,2,3)
  - Math.min(1,2,3)
  - Math.random(1,2,3)
  - Math.sqrt(64)
  - Math.log()
  - Math.PI
```

- continue from working with string.
